# frozen_string_literal: true

source 'https://rubygems.org'

gem 'sinatra'
gem 'rackup'
gem 'puma'   # For concurrent requests
gem 'httpparty'

group :test do
  gem 'rspec'
  gem 'rack-test'
  gem 'webmock'
end
